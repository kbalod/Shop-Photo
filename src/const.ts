export enum APIRoute {
    Main = '/',
    Cameras = '/cameras',
    Reviews = '/reviews',
    Similar = '/similar',
    Promo = '/promo'
  }

export enum AppRoute {
    Main = '/',
    Camera = '/offer/:id',
  }
export enum NameSpace {
    Cameras = 'CAMERAS',
    Camera = 'CAMERA',
    Errors = 'ERRORS',
  }
